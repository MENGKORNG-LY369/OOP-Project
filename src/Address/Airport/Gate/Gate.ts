export class Gate {
    constructor
        (
            private gateNumber: string,
            private status: GateStatus


        ) {

        this.gateNumber = gateNumber
        this.status = status
    }
    method(type): type{
        
    }

}